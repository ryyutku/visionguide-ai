# Core Computer Vision and ML
opencv-python-headless>=4.6.0
--extra-index-url https://download.pytorch.org/whl/cpu
torch>=2.0.0
torchvision>=0.15.0

# YOLO and Dependencies (Install ultralytics separately with --no-deps)
# Then install these manually:
polars>=0.20.0
psutil>=5.8.0
scipy>=1.4.1
ultralytics-thop>=2.0.18

# Raspberry Pi GPIO
RPi.GPIO>=0.7.0

# Image Processing
Pillow>=9.0.0

# Web Framework (for GUI/API)
Flask>=2.0.0

# GUI (if running on laptop with display)
# tkinter is built into Python, but these help:
# matplotlib>=3.5.0  # Uncomment if you need plotting

# Utilities
numpy>=1.21.0
pyyaml>=6.0
tqdm>=4.65.0
requests>=2.28.0

# Logging and Monitoring
# (logging is built-in, no install needed)